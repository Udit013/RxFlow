# RxFlow Web — production env (set in the Vercel dashboard → Project → Settings → Environment Variables)
# Point at your deployed Render API (note the /api/v1 suffix).
NEXT_PUBLIC_API_URL=https://rxflow-api.onrender.com/api/v1
